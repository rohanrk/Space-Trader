<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.communistutopia.spacetrader">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".view.LoginActivity">
        </activity>
        <activity
                android:name=".GoogleLoginActivity"
                android:label="@string/title_activity_google_login">
        </activity>
        <activity android:name=".view.LoadingScreenActivity">
        </activity>
        <activity android:name=".view.SelectionActivity2">
        </activity>
        <activity android:name=".view.SelectionActivity">
        </activity>
        <activity android:name=".view.MarketplaceActivity">
        </activity>
        <activity android:name=".view.GameActivity">
        </activity>
        <activity android:name=".view.ConfigurationActivity">
        </activity>
        <activity android:name=".view.LocationActivity">
        </activity>
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>